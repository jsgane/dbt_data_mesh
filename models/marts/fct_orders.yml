models:
  - name: fct_orders
    config:
      contract:
        enforced: true
    columns:
      - name: ORDER_ID
        data_type: varchar
        constraints:
          - type: not_null
      - name: LOCATION_ID
        data_type: varchar
        #constraints:
        #  - type: not_null
      - name: CUSTOMER_ID
        data_type: varchar
        #constraints:
        #  - type: not_null
      - name: ORDER_TOTAL
        data_type: number
        #constraints:
        #  - type: not_null
      - name: TAX_PAID
        data_type: number
        #constraints:
        #  - type: not_null
      - name: ORDERED_AT
        data_type: TIMESTAMP_NTZ
        #constraints:
        #  - type: not_null
      - name: CUSTOMER_NAME
        data_type: varchar
        #constraints:
        #  - type: not_null
      - name: LOCATION_NAME
        data_type: varchar
        #constraints:
        #  - type: not_null
      - name: TAX_RATE
        data_type: number
        #constraints:
        #  - type: not_null
      - name: LOCATION_OPENED_AT
        data_type: TIMESTAMP_NTZ
        #constraints:
        #  - type: not_null
      - name: ORDERED_MONTH
        data_type: number
        #constraints:
        #  - type: not_null
      - name: ORDERED_DAY
        data_type: number
        #constraints:
        #  - type: not_null
      - name: ORDERED_YEAR
        data_type: number
        #constraints:
        #  - type: not_null